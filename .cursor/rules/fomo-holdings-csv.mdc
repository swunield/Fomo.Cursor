---
description: FOMO Top20 持仓 CSV 列顺序与显示格式约定
globs: fomo_top20_holdings_by_token.*
alwaysApply: true
---

# FOMO 持仓导出约定

## CSV 列顺序（固定，勿擅自调整）

```
代币名称,代币当前市值,总持仓价值,总持仓人数,人均持仓价值,代币最高市值,代币最高市值时间,最高持仓人,最高持仓价值,最低持仓人,最低持仓价值,所有持仓人,发射平台,合约地址
```

脚本常量：`update_token_marketcap.py` 中的 `CSV_COLUMNS`。写 CSV / Markdown / JSON 展示行时必须按此顺序。

## 显示格式

- **价值列**（总持仓价值、人均持仓价值、代币当前市值、代币最高市值）：`K` 整数 / `M` 一位小数（如 `887K`、`37.6M`）
- **最高/最低持仓价值**：`{价值}({占当前市值%})`，如 `12.1M(2.44%)`
- **持仓人列**（所有/最高/最低持仓人）：`排名.昵称`，如 `9.ogle`
