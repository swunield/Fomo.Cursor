---
description: FOMO Top20 持仓 CSV 列顺序与显示格式约定
globs: fomo_top20_holdings_by_token.*
alwaysApply: true
---

# FOMO 持仓导出约定

## CSV 列顺序（固定，勿擅自调整）

```
名称,市值,成交量,天数,24h涨跌,持仓市值,持仓人数,人均持仓市值,创建时间,最高持仓人,最高持仓市值,最低持仓人,最低持仓市值,所有持仓人,发射平台,合约地址
```

脚本常量：`update_token_marketcap.py` 中的 `CSV_COLUMNS`。写 CSV / Markdown / JSON 展示行时必须按此顺序。

## 显示格式

- **市值列**（人均持仓市值、市值、成交量）：`K` 整数 / `M` 一位小数（如 `887K`、`37.6M`）
- **24h涨跌**：带符号百分比，如 `-21.18%`、`+5.20%`
- **持仓市值 / 最高/最低持仓市值**：`{市值}({占市值%})`，如 `12.1M(2.44%)`
- **持仓人列**（所有/最高/最低持仓人）：`排名.昵称`，如 `9.ogle`
- **天数**：自创建时间起的天数，一位小数，如 `8.7`
- **创建时间**：来自 FOMO `tokenFilterResult.createdAt`（Unix 秒），展示为可读时间

## 行情来源

- 登录全量：FOMO `/v2/users/{id}/balances` 的 `tokenFilterResult`（市值/成交量/涨跌），写入 `fomo_token_meta_cache.json`
- 非登录快速：只用上述本地缓存，不请求 DexScreener/Gecko
